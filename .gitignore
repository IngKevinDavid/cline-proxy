# Build output
dist/

# Compiled binary
cline-proxy
cline-proxy.exe
cline-proxy-*

# Go workspace
*.test
*.out

# Local data
.cline-accounts.json
.cline-credentials.json
.zen-config.json
zen-stats.jsonl
cline-proxy.log
cline-proxy-stream.log
data/
override.md
# Local debug tool (kept locally, not uploaded)
capture.go
capture-logs/

# Secrets (docker-compose documents putting API_KEY/ADMIN_PASSWORD here)
.env
.env.*

# Battle-test / local-run data (contains real keys - never commit)
bt-data/

# State files if DATA_DIR points at the repo root
cline-seed.json
combos.json
.session-secret
requests.jsonl
