# Public deployment requires API_KEY and ADMIN_PASSWORD (the container refuses
# to start otherwise). Provide them in a sibling .env file.
# PROXY_PORT customizes the listen/mapped port (default 3457).
services:
  cline-proxy:
    build: .
    container_name: cline-proxy
    restart: unless-stopped
    ports:
      - "${PROXY_PORT:-3457}:${PROXY_PORT:-3457}"
    volumes:
      - ./data:/app/data
      # System-prompt override is off by default (APPLY_SYSTEM_PROMPT_OVERRIDE=false).
      # Uncomment and set APPLY_SYSTEM_PROMPT_OVERRIDE=true to enable:
      # - ./override.md:/app/override.md:ro
    environment:
      # Listen port inside the container (explicit -port flag wins over this)
      - PORT=${PROXY_PORT:-3457}
      # The single /v1 API key (Bearer / x-api-key); required for public deployment
      - API_KEY=${API_KEY:?set API_KEY in .env}
      # Admin panel login password; required for public deployment
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:?set ADMIN_PASSWORD in .env}
      # opencode zen keys, comma-separated (optional; can also be set in the admin panel)
      - ZEN_KEYS=${ZEN_KEYS:-}
      # Zen session harvester (see docs/zen-harvester.md): ZEN_HARVEST=0 disables
      # it; ZEN_HARVEST_INTERVAL_HOURS tunes the periodic re-mint interval.
      - ZEN_HARVEST=${ZEN_HARVEST:-}
      - ZEN_HARVEST_INTERVAL_HOURS=${ZEN_HARVEST_INTERVAL_HOURS:-}
      # Account pool strategy, default round_robin (fill / random)
      - POOL_STRATEGY=${POOL_STRATEGY:-round_robin}
      # Request-log switch, default on (metadata only, 10MB cap auto-wipe)
      - LOG_REQUESTS=${LOG_REQUESTS:-true}
      - LOG_FILE_MAX_MB=${LOG_FILE_MAX_MB:-10}
      # Strict model matching: unknown model names return 400 instead of silently
      # falling back to the default model (set false to restore the old catch-all)
      - STRICT_MODEL_MATCH=${STRICT_MODEL_MATCH:-true}
      # cline account seed file (auto-imported when the pool is empty, optional)
      - CLINE_ACCOUNTS_SEED_FILE=${CLINE_ACCOUNTS_SEED_FILE:-}
      # Route cline upstream through the egress proxy pool (optional)
      - CLINE_USE_PROXIES=${CLINE_USE_PROXIES:-false}
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:${PROXY_PORT:-3457}/health"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

  # Optional: Caddy reverse proxy with automatic HTTPS (recommended for public exposure).
  # Uncomment, set DOMAIN in .env, then `docker compose --profile tls up -d`.
  # caddy:
  #   image: caddy:2-alpine
  #   container_name: cline-proxy-tls
  #   restart: unless-stopped
  #   profile: tls
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   environment:
  #     - DOMAIN=${DOMAIN:?set DOMAIN in .env}
  #   volumes:
  #     - ./Caddyfile:/etc/caddy/Caddyfile:ro
  #     - caddy_data:/data
  #     - caddy_config:/config
  #   depends_on:
  #     - cline-proxy

# volumes:
#   caddy_data:
#   caddy_config:
